<%= form_for(@contrat) do |f| %>
  <% if @contrat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contrat.errors.count, "error") %> prohibited this contrat from being saved:</h2>

      <ul>
      <% @contrat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :startDate %><br>
    <%= f.datetime_select :startDate %>
  </div>
  <div class="field">
    <%= f.label :endDate %><br>
    <%= f.datetime_select :endDate %>
  </div>
  <div class="field">
    <%= f.label :offre_id %><br>
    <%= f.number_field :offre_id %>
  </div>
  <div class="field">
    <%= f.label :offreur_id %><br>
    <%= f.number_field :offreur_id %>
  </div>
  <div class="field">
    <%= f.label :demandeur_id %><br>
    <%= f.number_field :demandeur_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
